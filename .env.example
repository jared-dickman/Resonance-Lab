# Resonance Lab Environment Variables
# Copy to frontend/.env.local for local dev
# Copy to frontend/.env.development for Vercel preview
# Copy to frontend/.env.production for Vercel prod

# Backend API (same key name, different values per env)
API_BASE_URL=http://localhost:8080
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080

# Supabase (same key name, different values per env)
SUPABASE_URL=https://xxxxxxxxxxxxx.supabase.co
SUPABASE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Anthropic (for agent-chat)
ANTHROPIC_API_KEY=sk-ant-api03-...

# Optional: LogRocket session replay
NEXT_PUBLIC_LOGROCKET_APP_ID=
