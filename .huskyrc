# Husky configuration to ensure hooks always run
# This prevents hooks from being skipped

# Ensure hooks run even in CI environments
export HUSKY=0

# Force color output in hooks
export FORCE_COLOR=1

# Ensure hooks run with proper shell
export SHELL=/bin/bash